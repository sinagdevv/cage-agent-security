# CAGE Python SDK

Client library for instrumenting and connecting AI agent applications to CAGE.

## Planned Features

- Automatic context propagation and causal edge reporting.
- Lightweight decorator `@cage.guard` for tool definitions.
- Attestation payload generation for intent and agent credentials.
- Direct integration hooks for LangChain, LlamaIndex, and custom agent loops.
